import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class AuthService {
  constructor() {}

  // La autenticación ahora se maneja en el backend
  // Este servicio ya no es necesario, pero lo mantenemos por compatibilidad
}
